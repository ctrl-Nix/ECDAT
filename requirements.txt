# Core tree-sitter (pinned for Python 3.11 compatibility)
# REQUIRES: Python 3.11 ONLY
# tree-sitter-languages has no wheels for Python 3.12+.
# Use the Dockerfile (FROM python:3.11-slim) or install Python 3.11 locally.
tree-sitter==0.21.3
tree-sitter-languages==1.10.2
PyYAML>=6.0

# FastAPI stack
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0
pydantic-settings>=2.3.0
python-multipart>=0.0.9

# Database
SQLAlchemy>=2.0,<2.1
psycopg2-binary>=2.9

# Utils
python-dotenv>=1.0
pytest>=8.0
httpx>=0.27.0
google-genai>=0.1.1